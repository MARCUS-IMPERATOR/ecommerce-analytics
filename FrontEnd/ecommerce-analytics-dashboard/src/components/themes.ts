import { yellow } from "@mui/material/colors";
import {COLORS} from "../styles/colors"

export const THEMES = {

    green : {
        iconBg: COLORS.lightGreen,
        iconColor: COLORS.textGreen,
        trendColor: COLORS.textGreen
    },

    orange: {
        iconBg: COLORS.lightOrange,
        iconColor: COLORS.orange,
        trendColor: COLORS.orange
    },

    purple: {
        iconBg: COLORS.lightPurple,
        iconColor: COLORS.purple,
        trendColor: COLORS.purple
    },

    blue: {
        iconBg: COLORS.lightBlue,
        iconColor: COLORS.blue,
        trendColor: COLORS.blue
    },

    gold: {
        iconBg: COLORS.lightGold,
        iconColor: COLORS.gold,
        trendColor: COLORS.gold
    },

    yellow : {
        iconBg: COLORS.lightDeepYellow,
        iconColor: COLORS.deepYellow
    }

    
};
